const applicationConfig = {
    // If true, use the real Firebase instance in the cloud. If false,
    // use a local emulator
    isProduction: false,

    // The name of this application
    appName: "model-collab",
};

export default applicationConfig;