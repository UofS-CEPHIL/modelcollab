export interface FirebaseColorProperties {
    color: string;
}

export interface FirebaseTextProperties {
    fontSize: number;
    bold: boolean;
    italic: boolean;
    underline: boolean;
    text: string;
}
